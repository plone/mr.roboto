[buildout]
extends =
    jenkins.cfg
always-accept-server-certificate = true
parts += code-analysis

[code-analysis]
recipe = plone.recipe.codeanalysis
directory = ${directory_of_package}
jenkins = True
flake8 = True
jshint = True
csslint = True

[versions]
zc.buildout = 1.7.1

${structure: sources}